# GoCleaner 实现与测试文档

> 版本：v1.0 草稿  
> 日期：2026-06-13  
> 阶段：第 14-19 天 实现、测试、体验完善和交付整理

---

## 1. 本阶段目标

第 14-15 天实现两个独立的高风险演示功能：

```text
HKCU Run 无效启动项扫描 -> 注册表备份 -> 手动删除选中值
用户手动选择单个普通文件 -> 1/3/7 次覆写 -> flush/sync -> 随机重命名 -> 删除
```

这两个功能均不进入普通“一键清理”流程。注册表项和文件粉碎都必须通过独立入口触发，执行前显示风险提示并二次确认，失败原因写入结果和操作日志。

## 2. 已联调功能

| 功能点 | 实现说明 | 验收要点 |
| --- | --- | --- |
| 注册表扫描 | 新增 `ScanInvalidStartupRegistry()`，只读取 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` | 不做全注册表扫描，不读取 HKLM |
| 启动命令解析 | 支持 `REG_SZ` / `REG_EXPAND_SZ`，解析引号路径、环境变量路径和参数 | 相对命令、系统命令、不明确目标不误报 |
| 注册表扫描项 | 返回 `type=registry`、`category=registry`、`risk=high`、`selected=false` | 高风险默认不勾选 |
| 注册表备份删除 | 新增 `DeleteRegistryItems(items, confirmed)` | 删除前写入 `data/registry_backup/*.reg`，备份失败拒绝删除 |
| 注册表失败反馈 | 删除成功/失败均返回路径、值名、备份路径和原因 | 前端结果面板和操作日志可见 |
| 文件选择 | 新增 `SelectShredFile()`，通过 Wails 文件选择器选单个文件 | 不支持批量扫描或目录粉碎 |
| 文件粉碎 | 新增 `ShredFile(request, confirmed)`，仅允许 1 / 3 / 7 次覆写 | 拒绝空路径、目录、符号链接、不存在路径和非法 passes |
| 粉碎流程 | 每次覆写后 `Sync()`，最后随机重命名并删除 | 成功返回粉碎数量和释放大小，失败返回原因 |
| 前端入口 | 增加“扫描注册表”“备份并删除注册表项”“选择文件粉碎” | 注册表和普通文件不能走同一个清理按钮 |
| 操作日志 | 新增 `registry_backup`、`registry_delete`、`shred` 日志 | 保留失败路径、失败原因和耗时 |

## 3. 风险提示文案

UI 和报告固定展示以下限制：

```text
注册表删除可能影响应用自启动。
文件粉碎是高风险操作。SSD、NTFS 日志、系统缓存、云同步目录等场景下无法保证专业取证级不可恢复。
```

注册表删除前会提示作用范围仅为 HKCU Run 无效启动项，并说明会先导出 `.reg` 备份。文件粉碎前会提示目标文件路径、覆写次数，以及粉碎后无法通过本程序恢复。

## 4. 演示流程

1. 启动 GoCleaner，点击“开始扫描”完成普通文件和插件扫描。
2. 点击“扫描注册表”，确认只新增“注册表”分类下的高风险项，且默认不勾选。
3. 勾选一个注册表项，点击“备份并删除注册表 N 项”，完成两次确认。
4. 查看“注册表结果”，确认展示删除数量、备份路径和失败原因。
5. 点击“选择文件粉碎”，选择测试目录中的普通文件。
6. 在覆写次数中选择 1 / 3 / 7，点击“确认粉碎”，完成两次确认。
7. 查看“粉碎结果”和“操作日志”，确认 `registry_backup`、`registry_delete`、`shred` 记录可见。

推荐演示数据使用测试注册表值和临时文件，不使用真实业务软件的启动项或重要文件。

## 5. 测试记录

| 测试项 | 命令 | 期望 |
| --- | --- | --- |
| Go 单元测试 | `go test ./...` | 注册表解析/备份、粉碎流程、App 日志等测试通过 |
| Go 静态检查 | `go vet ./...` | 无 vet 诊断 |
| 前端汇总逻辑 | `npm run test:summary` | registry high-risk 统计、普通清理和注册表动作隔离通过 |
| 前端构建 | `npm run build` | TypeScript 和 Vite 构建通过 |

真实注册表读写测试默认跳过，仅在显式设置 `GOCLEANER_RUN_REAL_REGISTRY_TESTS=1` 时写入 HKCU 测试 key。

### 5.1 第 18 天测试用例矩阵

| 测试类别 | 测试文件 | 代表测试函数 | 测试数据 | 预期结果 | 风险控制点 |
| --- | --- | --- | --- | --- | --- |
| 文件扫描测试 | `internal/scanner/scanner_test.go` | `TestScan_PatternMatching`、`TestScan_TotalSizeEqualsSumOfFileSizes`、`TestScan_HighRiskDefaultUnselected` | `t.TempDir()` 中构造 `.tmp`、`.log`、嵌套目录和高风险规则 | 匹配正确文件、统计大小正确、高风险项默认不选 | 不扫描真实系统目录，不破坏用户文件 |
| 文件扫描测试 | `internal/scanner/scanner_test.go` | `TestScan_WildcardPathExpansion`、`TestScan_ExcludeFiltering`、`TestScan_MinAgeDaysFilter` | 临时目录模拟浏览器 Profile、排除目录、新旧文件时间 | 通配路径展开、排除项生效、只返回满足年龄条件的文件 | 规则行为可重复验证 |
| 删除失败测试 | `internal/cleaner/cleaner_test.go` | `TestCleanRecordsMissingFileFailure`、`TestCleanRejectsUnsupportedItemType` | 不存在文件、目录项 | 不删除，返回 `FailedFiles` 和 `FailedReasons` | 失败必须可见，不静默忽略 |
| 删除失败测试 | `internal/cleaner/cleaner_windows_test.go` | `TestCleanSkipsLockedFileAndRecordsReason` | Windows 上保持文件句柄占用 | 清理跳过被占用文件并记录 locked/in-use 原因 | 避免强制删除占用文件 |
| 插件扫描测试 | `internal/scanner/plugin_scanner_test.go` | `TestScanBrowserPluginsReadsChromeManifestAndLocaleName`、`TestScanBrowserPluginsUsesNewestVersionDirectory` | 临时目录模拟 Chrome / Edge `Extensions` 结构和 manifest | 正确读取插件名、版本、Profile，选择最新版本目录 | 插件只扫描展示，不直接删除 |
| 插件扫描测试 | `internal/scanner/plugin_scanner_test.go` | `TestScanBrowserPluginsRecordsBrokenManifestAndContinues` | 损坏 manifest 与正常插件并存 | 坏 manifest 进入错误列表，正常插件继续扫描 | 单项失败不影响整体扫描 |
| 注册表备份测试 | `internal/registry/backup_test.go` | `TestWriteRegistryBackupEscapesStringValues`、`TestWriteRegistryBackupEncodesExpandStringValues` | 构造 HKCU Run 扫描项和字符串值 | `.reg` 文件正确转义普通字符串和 ExpandString | 注册表修改前可恢复 |
| 注册表备份测试 | `internal/registry/backup_test.go` | `TestDeleteRegistryItemsWithBackupFailureStopsBeforeDelete` | 不可写备份目录 | 备份失败时返回失败原因，不执行删除 | 备份失败拒绝修改注册表 |
| 文件粉碎测试 | `internal/cleaner/shredder_test.go` | `TestShredFileOverwritesAndDeletesRegularFile`、`TestShredFileRequiresConfirmation` | 临时普通文件 | 确认后覆写、重命名并删除；未确认时报错且文件保留 | 高风险操作必须确认 |
| 文件粉碎测试 | `internal/cleaner/shredder_test.go` | `TestShredFileRejectsInvalidPassesAndDirectories`、`TestShredFileRejectsMissingFilesAndSymlinks` | 非法 passes、目录、不存在路径、符号链接 | 拒绝处理并记录失败原因 | 不处理目录、符号链接和异常路径 |
| 前端结果状态测试 | `frontend/tests/summary.test.ts` | `describeCleanOutcome`、`hasPermissionFailure` 相关断言 | 构造成功、失败、部分成功和权限不足结果 | 正确标记全部成功、部分成功、全部失败、空结果 | UI 反馈明确可解释 |

### 5.2 测试执行记录

| 命令 | 目的 | 通过标准 |
| --- | --- | --- |
| `go test ./...` | 运行 Go 后端全部单元测试 | 所有包测试通过，无失败用例 |
| `go vet ./...` | Go 静态检查 | 无 vet 诊断 |
| `npm run test:summary` | 前端汇总与结果状态逻辑测试 | TypeScript 编译通过，断言无失败 |
| `npm run build` | 前端生产构建 | `tsc` 与 Vite 构建成功 |

第 18 天默认不执行真实系统路径破坏性测试。真实注册表读写测试仅在显式设置 `GOCLEANER_RUN_REAL_REGISTRY_TESTS=1` 时启用。

## 6. 风险控制说明

- 扫描先于清理：注册表删除只能从扫描结果中选择项，文件粉碎只能手动选择单个文件。
- 预览先于删除：注册表项展示 hive、key、value、原始数据和目标路径；文件粉碎展示路径和覆写次数。
- 高风险默认不选：注册表扫描项固定 `risk=high` 且 `selected=false`。
- 注册表先备份再修改：`.reg` 备份写入失败时直接拒绝删除。
- 删除范围受限：注册表仅删除选中 value，不删除整个 key；不做全注册表扫描或自动修复。
- 粉碎范围受限：只处理普通文件，拒绝目录、符号链接、空路径和不存在路径。
- 失败必须可见：备份失败、删除失败、粉碎失败均返回路径和原因。
- 操作必须留痕：注册表备份、注册表删除、文件粉碎均写入 `data/operation.jsonl`。

## 7. 第 16-17 天体验完善记录

本阶段围绕异常可见性和演示体验做完善，不扩大清理范围，不新增无确认的深度清理能力。

| 功能点 | 实现说明 | 验收要点 |
| --- | --- | --- |
| 权限不足提示 | 扫描阶段将 `Access is denied` / `permission denied` 归类为“权限不足”，前端在扫描和处理结果中显示恢复建议 | 不静默忽略失败路径，失败原因进入结果面板和操作日志 |
| 扫描进度 | 新增 `ScanProgress` 事件载荷，通过 Wails 事件 `gocleaner:scan-progress` 推送加载规则、扫描文件、扫描插件、扫描注册表、完成等阶段 | UI 展示阶段、当前规则、发现项、失败项和近似百分比 |
| 清理确认弹窗 | 普通清理、插件隔离、注册表删除、文件粉碎均使用应用内确认弹窗 | 高风险普通清理、注册表删除和文件粉碎保留二次确认 |
| 空结果处理 | 未扫描、扫描后无项目、筛选后无匹配项使用不同文案 | 空结果不是错误，不引导用户执行危险操作 |
| 失败和部分成功 | 前端按成功数和失败数标记“全部成功 / 部分成功 / 全部失败 / 未处理任何项目” | 失败项保留在列表中并取消勾选，成功项从结果中移除 |

扫描进度为阶段式近似进度。程序不会为了精确百分比额外预扫描磁盘，避免增加扫描耗时和权限风险。

新增测试记录：

| 测试项 | 命令 | 覆盖点 |
| --- | --- | --- |
| 扫描进度单元测试 | `go test ./internal/scanner` | `ScanWithOptions` 进度回调、权限不足和文件占用分类 |
| 前端结果状态测试 | `npm run test:summary` | 空结果、全部成功、部分成功、全部失败、权限不足识别 |

## 8. 第 19 天报告和截图整理

第 19 天交付文档：

| 文档 | 作用 |
| --- | --- |
| `docs/系统分析文档.md` | 说明需求范围、风险边界、功能需求、非功能需求和交付标准 |
| `docs/系统设计文档.md` | 说明总体架构、模块职责、核心数据结构、流程图、风险控制和测试设计 |
| `docs/实现与测试文档.md` | 记录实现功能、风险提示、演示流程、测试用例矩阵和测试命令 |
| `docs/实训报告.md` | 面向答辩的完整报告，包含背景、需求、设计、实现、测试、截图和总结 |

截图素材统一放在 `docs/images/`：

| 文件 | 内容 | 备注 |
| --- | --- | --- |
| `01-main-ui.png` | 主界面、扫描入口、风险统计、结果表格和日志区 | 可用于展示整体工作台 |
| `02-confirm-dialog.png` | 清理确认弹窗 | 用于说明预览先于删除 |
| `03-result-states.png` | 结果状态、失败原因和权限提示 | 用于说明失败必须可见 |
| `04-test-output.png` | 测试命令或测试记录截图 | 用于说明测试证据 |

当前环境未注入 Wails 后端绑定，截图主要用于报告排版和答辩说明：主界面图根据 Vite 前端预览整理，确认弹窗和结果状态图根据第 16-17 天交互状态整理，测试输出图根据本轮验证命令输出整理。真实扫描、清理和日志页面截图应在 Wails 桌面环境中按同名清单补拍。

## 9. 第 20 天最终打包验证

第 20 天新增 GitHub Release 自动打包发布流程。发布 workflow 位于 `.github/workflows/release.yml`，支持两种触发方式：

```text
1. 推送 `v*` 版本标签，例如 `v1.0.0`。
2. 在 GitHub Actions 页面手动运行 Release workflow，并输入版本号。
```

workflow 会依次执行前端依赖安装、前端汇总逻辑测试、前端生产构建、Go 单元测试、Go vet、Wails Windows amd64 打包，并将 `build/bin/GoCleaner.exe` 重命名为 `GoCleaner-<version>-windows-amd64.exe` 后上传到 GitHub Release。

| 验证项 | 命令或操作 | 通过标准 |
| --- | --- | --- |
| Go 单元测试 | `go test ./...` | 全部测试通过 |
| Go 静态检查 | `go vet ./...` | 无 vet 诊断 |
| 前端汇总逻辑测试 | `npm run test:summary` | TypeScript 编译和断言通过 |
| 前端生产构建 | `npm run build` | Vite 构建成功 |
| GitHub Release 打包 | 推送 `v1.0.0` tag 或手动运行 Release workflow | Release 附件生成 `GoCleaner-v1.0.0-windows-amd64.exe` |
| exe 冒烟测试 | 下载 Release exe 后启动 | 能扫描、预览、确认清理、展示失败、记录日志 |

Release exe 冒烟测试清单：

```text
1. 程序能启动。
2. 点击开始扫描后能展示扫描结果。
3. 高风险项目默认不勾选。
4. 低风险临时文件能在确认后清理。
5. 清理失败时能看到失败原因。
6. 操作日志页面能看到扫描或清理记录。
7. 插件扫描、注册表扫描、文件粉碎入口可演示。
```

### 9.1 Actions skipped 说明

此前 `CI` workflow 中的 `Package Windows app` job 带有如下 job-level 条件：

```yaml
if: github.event_name == 'workflow_dispatch' || github.event_name == 'release'
```

因此在普通 `push` 或 `pull_request` 触发 CI 时，测试 job 会运行，但打包 job 会显示 `This job was skipped`。这不是打包失败，而是触发条件不满足。

当前已将 `CI` workflow 收敛为测试和静态检查，不再保留旧的打包 job。第 20 天正式交付改为使用独立的 `Release` workflow 完成自动打包发布：

```text
1. 推送 `v*` tag，例如 `v1.0.0`，自动创建或更新 GitHub Release。
2. 或在 GitHub Actions 页面手动运行 `Release` workflow，输入版本号。
```

普通提交只负责测试和静态检查；正式 exe 附件以 `Release` workflow 的运行结果为准。

### 9.2 Windows 下载信任和管理员权限说明

当前课程交付版生成的是未签名 exe。从 GitHub 下载后，Windows 可能因为“未知发布者”、SmartScreen 信誉不足或文件带有 Internet 下载标记而提示不信任。要在正式产品中减少或避免该提示，需要购买代码签名证书，对 exe 或安装包进行 Authenticode 签名，并在 GitHub Actions 中使用安全保存的证书或云签名服务完成签名和时间戳；单纯修改程序代码不能消除未签名程序的系统信任提示。

当前 `build/windows/wails.exe.manifest` 未配置 `requestedExecutionLevel`，程序默认以普通用户权限运行。涉及系统目录、注册表或被权限保护的路径时，程序会展示权限不足等失败原因。答辩演示如需访问受保护路径，可右键以管理员身份运行。若后续产品化需要自动 UAC 提权，可以选择在 manifest 中设置 `requireAdministrator`，或实现单独的高权限 helper 进程；课程版本优先保留普通权限启动，避免整个界面进程长期处于管理员权限下。

真实发布动作需要在代码提交并推送到 GitHub 后完成。本地阶段先验证 workflow 文件、测试命令和构建命令；GitHub Release 附件以远端 Actions 运行结果为准。
